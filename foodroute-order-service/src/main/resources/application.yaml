server:
  port: 0

spring:
  application:
    name: foodroute-order-service

  datasource:
    url: jdbc:mysql://localhost:3306/foodroute_order
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
